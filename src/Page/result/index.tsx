import * as _ from './styled';
import Button from '@/components/button';

const Result = () => {
  return (
    <_.Container>
      <_.HeaderText color="blue" size={24}>
        최종결과
      </_.HeaderText>
    </_.Container>
  );
};
